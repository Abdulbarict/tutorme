rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows ANY logged-in user to read and write database records
      allow read, write: if request.auth != null;
    }
  }
}
